# include <stdio.h>
int main(){
    int num;
        printf("Enter any Number:");
        scanf("%d",&num);

        (num%2==0)?
        printf("The given number is Even number"):
        printf("The given number is odd number");

        return 0 ;
}